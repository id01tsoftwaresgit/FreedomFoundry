// FreedomFoundry Main JavaScript File
// Alpine.js is included via CDN in the HTML for simplicity and performance.

document.addEventListener('alpine:init', () => {
  // You can define Alpine.js data and stores here if needed.
  // For example:
  // Alpine.data('myComponent', () => ({
  //   message: 'Hello from my component!'
  // }))
  console.log('Alpine.js has been initialized.');
});
